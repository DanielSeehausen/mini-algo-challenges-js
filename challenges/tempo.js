// You are not restricted to the pre-named/defined functions. Write as many or as few as you want.

/* For this challenge we want to flatten a nested array
 * We don't know how many nested levels are in the array!
 * i.e. arr could be [[1], [2, 3]] or [[1, [2, 3], 4], [[[5], 6], [7], 8], [9]]
 *
 * Once you have a working solution, refactor to implement the following (if not already present)
 * 1.) Solve recursively
 */

function flattenArray(arr) {
  // don't assume any particular format or depth to the array
}
